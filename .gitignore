# Config locale (secrets, tokens, chemins machine)
brain-compose.local.yml

# Brain state (reconstruit par brain-engine)
brain.db
brain.db-wal
brain.db-shm

# Python cache (brain-engine + scripts)
__pycache__/
brain-engine/.venv/
brain-engine/*.log
brain-engine/viz_cache.json
.brain-engine.pid

# Brain-ui build + deps
brain-ui/node_modules/
brain-ui/dist/

# Satellites — visibles dans le template, gitignores apres ownership declaration
# (bash scripts/ownership.sh pour basculer)
# toolkit/
# progression/
# reviews/
# profil/
# todo/
# claims/
# workspace/

# OS
.DS_Store
Thumbs.db
